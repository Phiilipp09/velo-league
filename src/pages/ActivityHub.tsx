import { useEffect, useState } from 'react'
import { CheckCircle2, ChevronRight, Link2, Plus, ShieldCheck, Trash2, Unlink, X } from 'lucide-react'
import { Card, SectionHeading } from '../components/Ui'
import { deleteRide, getOwnRides, saveRide, type LiveRide } from '../lib/supabaseData'
import { announceLiveDataChange } from '../lib/liveSeason'
import { getSupabaseAccessToken } from '../lib/supabaseAuth'
type Ride={id:string;title:string;distance:number;elevation:number;points:number;source:'manual'|'strava'}
const mapRide=(ride:LiveRide):Ride=>({id:ride.id,title:ride.title,distance:ride.distance_m/1000,elevation:ride.elevation_m,points:ride.points,source:ride.source})
export function ActivityHub({notify,userId}:{notify:(message:string)=>void;userId?:string}){const[rides,setRides]=useState<Ride[]>([]),[connected,setConnected]=useState(false),[manual,setManual]=useState(false),[detail,setDetail]=useState<Ride|null>(null);const token=()=>({Authorization:`Bearer ${getSupabaseAccessToken()||''}`});const load=async()=>{if(userId)setRides((await getOwnRides(userId)).map(mapRide))};useEffect(()=>{void load();void fetch('/api/strava/status',{headers:token()}).then(r=>r.json()).then(x=>setConnected(Boolean(x.connected)))},[userId]);const save=async(title:string,distance:number,elevation:number,source:'manual'|'strava'='manual')=>{if(!userId)return;const points=Math.round(distance+elevation/10);const groupId=localStorage.getItem(`velo-active-group:${userId}`);try{const ride=await saveRide({user_id:userId,group_id:groupId,source,title,distance_m:Math.round(distance*1000),elevation_m:elevation,points,started_at:new Date().toISOString()});setRides(current=>[mapRide(ride),...current]);setManual(false);announceLiveDataChange();notify(`Fahrt gespeichert · +${points} Saisonpunkte`)}catch(error){notify(error instanceof Error?error.message:'Fahrt konnte nicht gespeichert werden.')}};const remove=async()=>{if(!detail)return;try{await deleteRide(detail.id);setRides(current=>current.filter(ride=>ride.id!==detail.id));setDetail(null);announceLiveDataChange();notify('Fahrt gelöscht.')}catch(error){notify(error instanceof Error?error.message:'Fahrt konnte nicht gelöscht werden.')}};const connect=async()=>{const res=await fetch('/api/strava/connect',{headers:token()}),data=await res.json();if(data.url)window.location.assign(data.url);else notify(data.error||'Strava konnte nicht verbunden werden.')};return <div className="page activities-page"><Card className="strava-card"><div className="strava-logo">STRAVA</div><div className="strava-copy"><p className="eyebrow">DEIN AKTIVITÄTS-IMPORT</p><h2>{connected?'Strava ist verbunden':'Verbinde dein Strava'}</h2><p>Die Verbindung und deine Fahrten gehören ausschließlich deinem Konto.</p></div><div className="strava-action">{connected?<button onClick={()=>void fetch('/api/strava/disconnect',{method:'POST',headers:token()}).then(()=>setConnected(false))} className="text-button"><Unlink size={14}/> Strava trennen</button>:<button onClick={()=>void connect()} className="primary-button"><Link2 size={16}/> Mit Strava verbinden</button>}<small><ShieldCheck size={12}/> Sicher über Strava OAuth</small></div></Card><SectionHeading eyebrow="DEINE FAHRTEN" title="Aktivitäten" action={<button className="text-button" onClick={()=>setManual(true)}><Plus size={15}/> Fahrt eintragen</button>}/>{rides.length?<div className="ride-list">{rides.map(ride=><button className="ride-button" key={ride.id} onClick={()=>setDetail(ride)}><Card className="ride-card"><div className="ride-map"/><div className="ride-main"><p className="eyebrow">{ride.source==='manual'?'MANUELL':'STRAVA'}</p><h3>{ride.title}</h3><div><span>{ride.distance.toLocaleString('de-DE')} km</span><span>{ride.elevation.toLocaleString('de-DE')} hm</span></div></div><strong className="ride-points">+{ride.points}<small> PTS</small></strong><ChevronRight size={18}/></Card></button>)}</div>:<Card className="empty-inline"><Plus size={20}/><div><strong>Noch keine Fahrten.</strong><p>Deine privaten Aktivitäten erscheinen nur hier.</p></div></Card>}{manual&&<Manual onClose={()=>setManual(false)} onSave={save}/>} {detail&&<div className="modal-backdrop group-modal"><section className="create-group"><button className="drawer-close" onClick={()=>setDetail(null)}><X/></button><h2>{detail.title}</h2><p>{detail.distance} km · {detail.elevation} hm · +{detail.points} PTS</p><button className="secondary-button" onClick={()=>void remove()}><Trash2 size={15}/> Fahrt löschen</button></section></div>}</div>}
function Manual({onClose,onSave}:{onClose:()=>void;onSave:(title:string,distance:number,elevation:number)=>void}){const[name,setName]=useState('Sonntagsrunde'),[km,setKm]=useState(45),[hm,setHm]=useState(620);return <div className="modal-backdrop group-modal"><section className="create-group"><button className="drawer-close" onClick={onClose}><X/></button><h2>Fahrt hinzufügen</h2><label>Name der Fahrt<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Kilometer<input type="number" value={km} onChange={e=>setKm(Number(e.target.value))}/></label><label>Höhenmeter<input type="number" value={hm} onChange={e=>setHm(Number(e.target.value))}/></label><button className="primary-button" onClick={()=>onSave(name,km,hm)}>Fahrt speichern</button></section></div>}
