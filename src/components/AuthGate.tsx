import { useMemo, useState } from 'react'
import { ArrowRight, Bike, CalendarDays, Check, LockKeyhole, Mail, UserPlus } from 'lucide-react'
import { isSupabaseConfigured, signInWithSupabase, signUpWithSupabase } from '../lib/supabaseAuth'
type Account = { email: string; password: string; name: string; birthDate: string }
const accountKey = 'velo-test-account', sessionKey = 'velo-session'
export function getStoredSession() { return localStorage.getItem(sessionKey) ?? sessionStorage.getItem(sessionKey) }
export function AuthGate({ onAuthenticated }: { onAuthenticated: (name: string) => void }) { const [mode, setMode] = useState<'login' | 'register'>('login'), [email, setEmail] = useState(''), [password, setPassword] = useState(''), [name, setName] = useState(''), [birthDate, setBirthDate] = useState(''), [remember, setRemember] = useState(true), [error, setError] = useState(''), [loading, setLoading] = useState(false); const rules = useMemo(() => [{ label: 'Mindestens 8 Zeichen', met: password.length >= 8 }, { label: 'Mindestens ein Großbuchstabe', met: /[A-ZÄÖÜ]/.test(password) }, { label: 'Mindestens eine Zahl', met: /\d/.test(password) }], [password]); const start = (displayName: string, birthday = '') => { if (birthday) localStorage.setItem('velo-profile-draft', JSON.stringify({ birthDate: birthday })); if (remember) localStorage.setItem(sessionKey, displayName); else sessionStorage.setItem(sessionKey, displayName); onAuthenticated(displayName) }; const submit = async () => { const cleanEmail = email.trim().toLowerCase(); if (!cleanEmail || !password || (mode === 'register' && (!name.trim() || !birthDate))) return setError('Bitte fülle alle Felder aus.'); if (mode === 'register' && !rules.every(rule => rule.met)) return setError('Bitte erfülle alle Passwortanforderungen.'); setLoading(true); setError(''); if (isSupabaseConfigured) { const result = mode === 'register' ? await signUpWithSupabase(cleanEmail, password, name.trim(), birthDate) : await signInWithSupabase(cleanEmail, password); setLoading(false); if (result.error) return setError(result.error.message || 'Anmeldung nicht möglich.'); const displayName = result.user?.user_metadata?.display_name || (mode === 'register' ? name.trim() : result.user?.email?.split('@')[0]) || 'Rider'; if (mode === 'register') localStorage.setItem('velo-new-user', 'true'); start(displayName, mode === 'register' ? birthDate : ''); return } setLoading(false); if (mode === 'register') { const account: Account = { email: cleanEmail, password, name: name.trim(), birthDate }; localStorage.setItem(accountKey, JSON.stringify(account)); localStorage.setItem('velo-new-user', 'true'); start(account.name, birthDate); return } const raw = localStorage.getItem(accountKey), account = raw ? JSON.parse(raw) as Account : null; if (!account || account.email !== cleanEmail || account.password !== password) return setError('E-Mail-Adresse oder Passwort stimmen nicht.'); start(account.name) }; const demo = () => { localStorage.removeItem('velo-new-user'); localStorage.setItem('velo-profile-complete:Philipp', 'true'); localStorage.setItem(sessionKey, 'Philipp'); onAuthenticated('Philipp') }; return <main className="auth-page"><section className="auth-panel"><div className="auth-brand"><span><Bike size={21}/></span><strong>VELO <b>LEAGUE</b></strong></div><p className="eyebrow">PRIVATE CYCLING LEAGUES</p><h1>{mode === 'login' ? 'Willkommen zurück.' : 'Deine Saison beginnt hier.'}</h1><p className="auth-copy">Registriere dich und richte dein Fahrerprofil in wenigen Schritten ein.</p><div className="auth-tabs"><button className={mode === 'login' ? 'active' : ''} onClick={() => { setMode('login'); setError('') }}>Anmelden</button><button className={mode === 'register' ? 'active' : ''} onClick={() => { setMode('register'); setError('') }}>Registrieren</button></div><form className="auth-form" onSubmit={event => { event.preventDefault(); void submit() }}>{mode === 'register' && <><label>Name<div><UserPlus size={16}/><input autoComplete="name" value={name} onChange={event => setName(event.target.value)} placeholder="Dein Name"/></div></label><label>Geburtsdatum<div><CalendarDays size={16}/><input type="date" value={birthDate} onChange={event => setBirthDate(event.target.value)}/></div></label></>}<label>E-Mail-Adresse<div><Mail size={16}/><input type="email" autoComplete="email" value={email} onChange={event => setEmail(event.target.value)} placeholder="name@email.de"/></div></label><label>Passwort<div><LockKeyhole size={16}/><input type="password" autoComplete={mode === 'login' ? 'current-password' : 'new-password'} value={password} onChange={event => setPassword(event.target.value)} placeholder="Dein Passwort"/></div></label>{mode === 'register' && <div className="password-requirements">{rules.map(rule => <span className={rule.met ? 'met' : ''} key={rule.label}><i>{rule.met && <Check size={12}/>}</i>{rule.label}</span>)}</div>}<label className="remember-row"><input type="checkbox" checked={remember} onChange={event => setRemember(event.target.checked)}/><span>Angemeldet bleiben</span></label>{error && <p className="auth-error">{error}</p>}<button className="auth-submit" disabled={loading} type="submit">{loading ? 'Bitte warten…' : mode === 'login' ? <>Anmelden <ArrowRight size={17}/></> : <>Weiter zum Fahrerprofil <ArrowRight size={17}/></>}</button></form><div className="developer-entry"><span>Nur zum Testen</span><button type="button" onClick={demo}>Entwickler-Demo starten <ArrowRight size={15}/></button></div></section></main> }
