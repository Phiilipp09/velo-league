import { appendCookies, clearCookie, cookies, exchangeToken, getAppOrigin, tokenCookies } from './_shared.js'
export default async function handler(req, res) { const origin = getAppOrigin(req), query = req.query || {}; if (query.error || !query.code || !query.state || cookies(req).velo_strava_state !== query.state) { appendCookies(res, [clearCookie('velo_strava_state')]); return res.redirect(302, `${origin}/?strava=denied#activities`) } try { const tokens = await exchangeToken({ code: String(query.code), grant_type: 'authorization_code' }); appendCookies(res, [...tokenCookies(tokens), clearCookie('velo_strava_state')]); return res.redirect(302, `${origin}/?strava=connected#activities`) } catch { return res.redirect(302, `${origin}/?strava=error#activities`) } }
