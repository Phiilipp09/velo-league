import { useState } from 'react'
import { ArrowRight, Bike, Check, Link2, Trophy, Users } from 'lucide-react'
const steps=[{icon:Users,title:'Deine Gruppe',text:'Erstelle eine private Liga oder tritt über einen Link bei.'},{icon:Bike,title:'Fahrten verbinden',text:'Strava importiert deine Touren automatisch.'},{icon:Trophy,title:'Saison starten',text:'Sammelt Punkte, Trikots und gemeinsame Momente.'}]
export function Onboarding({onSkip,onFinish}:{onSkip:()=>void;onFinish:()=>void}) { const [step,setStep]=useState(0), item=steps[step], Icon=item.icon;return <div className="modal-backdrop onboarding-wrap"><section className="onboarding-card"><button onClick={onSkip} className="skip-button">Überspringen</button><div className="onboarding-progress">{steps.map((_,i)=><i key={i} className={i<=step?'done':''}/>)}</div><span className="onboarding-icon"><Icon size={28}/></span><p className="eyebrow">VELO LEAGUE EINRICHTEN</p><h2>{item.title}</h2><p>{item.text}</p>{step===0&&<div className="onboarding-actions"><button className="primary-button"><Users size={15}/> Gruppe erstellen</button><button className="secondary-button"><Link2 size={15}/> Link eingeben</button></div>}{step===1&&<button className="strava-onboarding">STRAVA verbinden</button>}<button className="next-button" onClick={()=>step===2?onFinish():setStep(step+1)}>{step===2?<><Check size={17}/> Los geht's</>:<>Weiter <ArrowRight size={17}/></>}</button></section></div> }
