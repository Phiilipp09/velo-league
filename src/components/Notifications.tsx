import { useState } from 'react'
import { Bell, Mountain, Swords, Trash2, X } from 'lucide-react'
import type { Page } from '../App'
type Note={id:number;page:Page;kind:'duel'|'ride'|'group';title:string;text:string}
const initial:Note[]=[{id:1,page:'challenges',kind:'duel',title:'Lukas fordert dich heraus',text:'Sprint durch Corvara · vor 1 Std.'},{id:2,page:'activities',kind:'ride',title:'Neue Fahrt synchronisiert',text:'+142 Saisonpunkte · vor 2 Std.'},{id:3,page:'group',kind:'group',title:'Max hat deinen Gruppenlink geöffnet',text:'Gerade eben'}]
export function Notifications({onClose,onNavigate}:{onClose:()=>void;onNavigate:(page:Page)=>void}){const[notes,setNotes]=useState(initial);const icon=(kind:Note['kind'])=>kind==='duel'?<Swords size={15}/>:kind==='ride'?<Mountain size={15}/>:<Bell size={15}/>;return <div className="notification-popover"><div className="notification-title"><h3>Benachrichtigungen</h3><button onClick={onClose}><X size={17}/></button></div>{notes.length===0?<p className="notification-empty">Keine neuen Benachrichtigungen.</p>:notes.map(n=><div className="note-row" key={n.id}><button className="note-open" onClick={()=>{onNavigate(n.page);onClose()}}><span className="notice-icon">{icon(n.kind)}</span><p><b>{n.title}</b><small>{n.text}</small></p></button><button className="note-delete" aria-label="Benachrichtigung löschen" onClick={()=>setNotes(notes.filter(x=>x.id!==n.id))}><Trash2 size={14}/></button></div>)}</div>}
